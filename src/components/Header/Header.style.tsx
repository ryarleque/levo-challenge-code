import styled from 'styled-components';

export const HeaderContainerStyled = styled.div`
  && {
    &.detail {
        width: 16rem;
        text-align: center;
        border-right: 3px solid forestgreen;
        height: 100vh;
        background: #e3e3e3; 
    }
  }
`;

export const TitleHeaderStyled = styled.h2`
  && {
    margin: 2rem;
    font-size: 2.5rem;
  }
`;

export const ItemStyled = styled.div`
  && {
    margin-top: 4rem;
    color: #a722a7;
    font-weight: bold;
  }
`;